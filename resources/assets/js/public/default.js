$(document).ready(function() {
    // ========== publics =========
    // ----- vars -----
    var w = $(window).width();
    var h = $(window).height();

    // ----- plugin Init -----
    $('.jqimgFill').imgLiquid();

    // ----- public -----
});

